export default render;

var render_tasks = require('../lib/statical-ghost/lib/task/');
//     stream = gh.stream,
//     md = gh.md,
//     Theme = gh.Theme,
//     pipeline = gh.pipeline;

function render(input, output){
  let tasks = render_tasks.getTasks()
}
